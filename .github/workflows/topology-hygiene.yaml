name: Topology Hygiene

on:
  push:
    branches: [dev, uat, main]
  pull_request:
    branches: [dev, uat, main]

jobs:
  topology-hygiene:
    uses: privilegedescalation/.github/.github/workflows/topology-hygiene.yaml@main
